#include <stdio.h>
/**
 * main - function that outputs with the prinf function
 * Return: it returns 0 indicating a successfull program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
