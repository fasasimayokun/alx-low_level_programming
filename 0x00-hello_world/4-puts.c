#include <stdio.h>
/**
 * main - a function that prints with the puts function
 * Return: it returns 0 which indicates a successful program
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
